{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run",
            "isBackground": true,
            "type": "shell",
            "command": "go build && ./game",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}